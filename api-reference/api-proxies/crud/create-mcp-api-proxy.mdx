---
title: "Create MCP API Proxy"
description: "Creates an MCP (Model Context Protocol) API proxy in a single call. Routing mode is required — TOOL_EXPOSE to publish existing proxies as tools, or PASSTHROUGH to forward to an external MCP server."
---

## Endpoint

```
POST /apiops/projects/{projectName}/apiProxies/mcp/{apiProxyName}/
```

Creates an API proxy of type `MCP`. See [MCP Gateway](/en/ai-gateway/mcp-gateway) for the concepts —
**Expose Tools** vs **Passthrough**, protocol versions, and the inline-server model.

## Authentication

Requires a Personal API Access Token.

### Header

```
Authorization: Bearer YOUR_TOKEN
```

Permission: `API_MANAGEMENT` × `MANAGE`.

## Request

### Headers

| Header | Value | Required |
|--------|-------|----------|
| Authorization | Bearer \{token\} | Yes |
| Content-Type | application/json | Yes |

### Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| projectName | string | Yes | Project name |
| apiProxyName | string | Yes | MCP API proxy name. Overrides any name carried in the body. |

### Request Body

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| relativePath | string | Yes | Client-facing path the gateway listens on (for example `/mcp/support`) |
| description | string | No | Free-text description |
| routingMode | string | **Yes** | `TOOL_EXPOSE` or `PASSTHROUGH`. Unlike the AI proxy create endpoint, there is **no implicit default** — the same fail-closed contract [Update MCP Routing](/api-reference/api-proxies/settings/update-mcp-routing) uses |
| protocolVersion | string | No | MCP protocol version (`2024-11-05`, `2025-06-18`, `2026-07-28`); blank keeps the null-safe default |
| backendMcpRefName | string | No | **Legacy, PASSTHROUGH only.** Name of an existing standalone MCP connection, resolved to its id within the project through the read-only compatibility surface. New integrations should leave this blank and configure an inline `backendServers` entry afterward through [Update MCP Routing](/api-reference/api-proxies/settings/update-mcp-routing) instead |

#### Full JSON Body Example (TOOL_EXPOSE)

```json
{
  "relativePath": "/mcp/support",
  "description": "Exposes the support API as MCP tools",
  "routingMode": "TOOL_EXPOSE"
}
```

#### Minimal Body Example (PASSTHROUGH)

```json
{
  "relativePath": "/mcp/github",
  "routingMode": "PASSTHROUGH"
}
```

`exposedTools` (TOOL_EXPOSE) and the inline `backendServers` list (PASSTHROUGH) aren't part of this
create call — configure them afterward with [Update MCP Routing](/api-reference/api-proxies/settings/update-mcp-routing),
the same "create first, route afterward" pattern [Create AI API Proxy](/api-reference/api-proxies/crud/create-ai-api-proxy)
uses for `aiRouting`.

## Response

### Success Response (200 OK)

```json
{
  "status": "SUCCESS",
  "deploymentResult": {
    "success": false,
    "detailList": []
  }
}
```

Creation does not deploy the proxy. Deploy it separately with
`POST /apiops/projects/{projectName}/apiProxies/{apiProxyName}/environments/{environmentName}/`.

### Error Responses

| HTTP Code | Condition |
|-----------|-----------|
| 400 | `apiProxyName` is empty, request body is missing, `relativePath` is empty, `routingMode` is missing, an API proxy with the same name already exists in the project, or `backendMcpRefName` does not match any connection in the project |
| 401 | Token is missing or invalid |
| 500 | Unexpected server error |

```json
{
  "status": "FAIL",
  "message": "routingMode value can not be empty! Must be TOOL_EXPOSE or PASSTHROUGH."
}
```

## Notes and Warnings

- **`routingMode` is required, with no default.** This mirrors the routing-update endpoint's
  fail-closed contract — there's no implicit mode to fall back to.
- **Servers are configured inline, not by reference.** A fresh PASSTHROUGH integration doesn't need
  `backendMcpRefName` at all: create the proxy, then add one or more inline MCP servers through
  [Update MCP Routing](/api-reference/api-proxies/settings/update-mcp-routing)'s `backendServers`
  field. `backendMcpRefName` only exists to point a new proxy at a connection created before this
  release, through the read-only legacy compatibility surface.
- **Proxy-level settings apply the same as any other proxy type** — CORS, cache, error templates,
  client route, trace and traffic-log settings.
- Policies (including MCP Tool Call / MCP Tool Invoke on other proxies) are attached the same way as
  for any other proxy, through `POST /apiops/projects/{projectName}/apiProxies/{apiProxyName}/policies/{policyName}/`.

## Related Documentation

- [Update MCP Routing](/api-reference/api-proxies/settings/update-mcp-routing)
- [Discover MCP Tools for a Policy](/api-reference/policies/crud/discover-mcp-policy-tools)
- [List API Proxies](/api-reference/api-proxies/crud/list-api-proxies)
- [Create A2A API Proxy](/api-reference/api-proxies/crud/create-a2a-api-proxy)
- [MCP Gateway](/en/ai-gateway/mcp-gateway)

---
title: "List A2A Connections"
description: "Lists all AI Gateway A2A outbound connections in the specified project. Secret fields (bearerToken, clientSecret) are returned masked as boolean presence flags."
---

## Endpoint

```
GET /apiops/projects/{projectName}/a2a-connections/
```

## Authentication

Requires a Personal API Access Token.

### Header

```
Authorization: Bearer YOUR_TOKEN
```

## Request

### Headers

| Header | Value | Required |
|--------|-------|----------|
| Authorization | Bearer \{token\} | Yes |

### Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| projectName | string | Yes | Project name |

### Query Parameters

None

## Response

### Success Response (200 OK)

```json
{
  "status": "SUCCESS",
  "resultList": [
    {
      "id": "6631a9f2e4b0a1c2d3e4f501",
      "projectId": "6631a9f2e4b0a1c2d3e4f000",
      "projectName": "MyProject",
      "name": "weather-agent",
      "description": "External weather A2A agent",
      "enabled": true,
      "endpoint": "https://agent.example.com/a2a",
      "a2aProtocolVersion": "1.0",
      "allowPrivateNetworkEgress": false,
      "authScheme": "BEARER",
      "bearerTokenSet": true,
      "credentialRef": null,
      "tokenEndpoint": null,
      "clientId": null,
      "scope": null,
      "clientSecretSet": false,
      "sslConfigRef": null,
      "agentCardCacheTtlSeconds": 300,
      "agentCardCacheEnabled": true,
      "discoveredSkillCount": 2,
      "discoveredSkills": [
        {
          "id": "get_forecast",
          "name": "getForecast",
          "description": "Returns the weather forecast for a location",
          "tags": ["weather", "forecast"]
        },
        {
          "id": "get_current",
          "name": "getCurrent",
          "description": "Returns the current weather for a location",
          "tags": ["weather"]
        }
      ],
      "skillsParsedAt": 1720080000000,
      "healthStatus": "HEALTHY",
      "lastHealthCheckAt": "2026-07-13T09:00:00Z",
      "healthMessage": null
    }
  ],
  "resultCount": 1
}
```

#### Response Fields

| Field | Type | Description |
|-------|------|-------------|
| status | string | Response status: `SUCCESS` or `FAILURE` |
| resultList | array | List of A2A outbound connection objects |
| resultCount | integer | Total number of connections returned |

#### A2A Connection Object Fields

| Field | Type | Description |
|-------|------|-------------|
| id | string | Internal connection ID (read-only) |
| projectId | string | Owning project ID (read-only) |
| projectName | string | Owning project name (read-only) |
| name | string | Connection name (unique within the project) |
| description | string | Connection description |
| enabled | boolean | Whether the connection is enabled |
| endpoint | string | A2A agent endpoint (AgentCard well-known URL / message endpoint) |
| a2aProtocolVersion | string | A2A protocol/adapter version (default `1.0`) |
| allowPrivateNetworkEgress | boolean | Whether private-network egress is permitted (admin-gated) |
| authScheme | string | Auth scheme: `BEARER`, `OAUTH2_CLIENT_CREDENTIALS`, `MTLS`, or `NONE` |
| bearerTokenSet | boolean | `true` if a bearer token is configured (the plaintext is never returned) |
| credentialRef | string | Credential reference for the OAuth2 client-credentials scheme |
| tokenEndpoint | string | OAuth2 token endpoint URL |
| clientId | string | OAuth2 client ID |
| scope | string | OAuth2 scope (space-delimited) |
| clientSecretSet | boolean | `true` if an OAuth2 client secret is configured (the plaintext is never returned) |
| sslConfigRef | string | SSL config reference for the mTLS scheme (placeholder) |
| agentCardCacheTtlSeconds | integer | AgentCard cache TTL in seconds (`0` = no cache; default `300`) |
| agentCardCacheEnabled | boolean | Whether AgentCard caching is enabled |
| discoveredSkillCount | integer | Number of skills discovered from the agent's AgentCard |
| discoveredSkills | array | Discovered skill objects (id, name, description, tags) |
| skillsParsedAt | integer | Epoch millis of the last successful AgentCard parse (`null` = never parsed) |
| healthStatus | string | Last health-probe status: `HEALTHY`, `DOWN`, or `UNKNOWN` (`null` = never probed) |
| lastHealthCheckAt | string | Timestamp of the last completed health probe |
| healthMessage | string | Short error description when `healthStatus` is `DOWN`; otherwise `null` |

### EnumStatus

- `SUCCESS` - Operation successful
- `FAILURE` - Operation failed

### Error Response (400 Bad Request)

```json
{
  "status": "FAILURE",
  "resultMessage": "Project with name (MyProject) is not found!"
}
```

### Error Response (401 Unauthorized)

```json
{
  "status": "FAILURE",
  "resultMessage": "Token is not valid!"
}
```

## cURL Example

```bash
curl -X GET \
  "https://demo.apinizer.com/apiops/projects/MyProject/a2a-connections/" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Notes and Warnings

- **Secret Masking**: `bearerToken` and `clientSecret` are never returned. Only the boolean
  presence flags `bearerTokenSet` and `clientSecretSet` indicate whether they are configured (INV-06).
- **Empty List**: If the project has no A2A outbound connections, an empty `resultList` is returned.
- **usedByRefs Omitted**: The list response does not populate `usedByRefs` (referencing proxies).
  Use [Get A2A Connection](/api-reference/a2a-connections/get-a2a-connection) for that field.
- **Case Sensitivity**: Project and connection names are case-sensitive in the path; name lookups
  are performed case-insensitively.

## Related Documentation

- [Get A2A Connection](/api-reference/a2a-connections/get-a2a-connection) - Get a single connection by name
- [Create A2A Connection](/api-reference/a2a-connections/create-a2a-connection) - Create or upsert a connection
- [A2A Outbound Connections API](/api-reference/a2a-connections) - Resource overview
- [Authentication Guide](/api-reference/getting-started/authentication) - How to obtain and use API tokens
- [Error Handling](/api-reference/getting-started/error-handling) - Error response formats

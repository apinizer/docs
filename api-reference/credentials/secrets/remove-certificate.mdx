---
title: "Remove Certificate"
description: "Removes the certificate assignment from a credential. The credential will no longer use the certificate for TLS/SSL connections or digital signatures. The credential is automatically deployed to all environments after the certificate is removed."
---

## Endpoint

```
DELETE /apiops/projects/{projectName}/credentials/{username}/certificate/
```

## Authentication

Requires a Personal API Access Token.

### Header

```
Authorization: Bearer YOUR_TOKEN
```

## Request

### Headers

| Header | Value | Required |
|--------|-------|----------|
| Authorization | Bearer {token} | Yes |

### Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| projectName | string | Yes | Project name |
| username | string | Yes | Username of the credential |

### Notes

- Credential must exist
- If no certificate is assigned, the operation still succeeds
- Certificate assignment is set to null

## Response

### Success Response (200 OK)

```json
{
  "success": true,
  "deploymentResult": {
    "success": true,
    "message": "Deployment completed successfully",
    "environmentResults": [
      {
        "environmentName": "production",
        "success": true,
        "message": "Deployed successfully"
      },
      {
        "environmentName": "staging",
        "success": true,
        "message": "Deployed successfully"
      }
    ]
  }
}
```

### Error Response (400 Bad Request)

```json
{
  "error": "bad_request",
  "error_description": "Credential (username: api-user) was not found!"
}
```

### Error Response (401 Unauthorized)

```json
{
  "error": "unauthorized_client",
  "error_description": "Invalid token"
}
```

### Error Response (404 Not Found)

```json
{
  "error": "not_found",
  "error_description": "Project(MyProject) was not found or user does not have privilege to access it!"
}
```

## cURL Example

```bash
curl -X DELETE \
  "https://demo.apinizer.com/apiops/projects/MyProject/credentials/api-user/certificate/" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Notes and Warnings

- **Credential Requirements**:
  - Credential must exist
  - Operation succeeds even if no certificate is currently assigned

- **Automatic Deployment**:
  - Credential is automatically deployed to all environments after certificate removal
  - Deployment results are returned in the response

- **No Impact on Certificate**:
  - The certificate itself is not deleted
  - Only the assignment to the credential is removed

## Permissions

- User must have **`IDENTITY` + `MANAGE`** permission in the project
- For automatic deployment, user must also have **`IDENTITY` + `DEPLOY_UNDEPLOY`** permission

## Related Documentation

- [Set Certificate](/api-reference/credentials/secrets/set-certificate) - Assign certificate to credential
- [Set Secret Key](/api-reference/credentials/secrets/set-secret-key) - Assign secret key to credential
- [List Credentials](/api-reference/credentials/crud/list-credentials) - List all credentials


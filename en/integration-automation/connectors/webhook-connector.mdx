---
title: 'Webhook Connector'
description: 'Send webhooks to external systems, configure webhook payloads, handle responses, and implement event notifications'
---

Webhook Connector enables sending webhooks to external systems. Webhooks are HTTP callbacks that notify external systems about events.

## Overview

Webhook Connector provides capabilities to send webhook notifications to external systems, enabling event-driven integration patterns.

<CardGroup cols={2}>
  <Card title="HTTP Webhooks" icon="globe">
    Send HTTP webhook requests
  </Card>
  <Card title="Custom Payloads" icon="code">
    Configure custom webhook payloads
  </Card>
  <Card title="Retry Logic" icon="rotate-right">
    Automatic retry on failures
  </Card>
  <Card title="Event Notifications" icon="bell">
    Event-driven notifications
  </Card>
</CardGroup>

## Configuration

### Webhook URL

Configure webhook endpoint:

**Configuration:**

* **URL**: Webhook endpoint URL
* **Method**: HTTP method (POST, PUT, etc.)
* **Headers**: Custom headers
* **Payload**: Webhook payload

**Example:**

```json
{
  "url": "https://api.example.com/webhooks/order-created",
  "method": "POST",
  "headers": {
    "Content-Type": "application/json",
    "X-Webhook-Signature": "${signature}"
  }
}
```

### Payload Configuration

Configure webhook payload:

**JSON Payload:**

```json
{
  "payload": {
    "event": "order.created",
    "timestamp": "${timestamp}",
    "data": {
      "orderId": "${orderId}",
      "customerId": "${customerId}",
      "total": "${orderTotal}"
    }
  }
}
```

### Authentication

Configure webhook authentication:

**Signature:**

```javascript
const payload = JSON.stringify(webhookPayload);
const signature = generateSignature(payload, secret);
```

**Bearer Token:**

```json
{
  "headers": {
    "Authorization": "Bearer ${webhookToken}"
  }
}
```

## Error Handling

### Retry Logic

Configure retry on failure:

**Retry Configuration:**

* **Max Retries**: Maximum retry attempts
* **Retry Interval**: Time between retries
* **Retry Strategy**: Retry strategy

## Best Practices

<CardGroup cols={2}>
  <Card title="Use HTTPS" icon="lock">
    Always use HTTPS for webhooks
  </Card>
  <Card title="Sign Payloads" icon="shield-halved">
    Sign webhook payloads for security
  </Card>
  <Card title="Handle Errors" icon="exclamation-triangle">
    Implement proper error handling
  </Card>
  <Card title="Idempotency" icon="repeat">
    Design idempotent webhook handlers
  </Card>
</CardGroup>

## Related Topics

<CardGroup cols={2}>
  <Card title="Webhook Connections" icon="plug" href="/en/integration-automation/connection-management/webhook-connections">
    Configure webhook connections
  </Card>
  <Card title="Task Flow Design" icon="diagram-project" href="/en/integration-automation/task-flow-design">
    Learn Task Flow design
  </Card>
</CardGroup>

---
title: "AI Gateway Quickstart"
description: "You can go from zero to your first AI Gateway request in three steps: connect a provider, point your OpenAI SDK at Apinizer with your Apinizer credential, and stream a chat completion."
---

import {Steps, Step} from '@site/src/components/Steps';
import {Card, CardGroup} from '@site/src/components/Card';

<Steps>
  <Step title="Add an LLM Provider Connection">
    Connect Apinizer to at least one LLM provider — cloud (OpenAI, Anthropic, Azure, Vertex, Bedrock) or self-hosted (vLLM, Ollama, Hugging Face TGI). The connection stores the provider's credentials encrypted, so your application never handles the real key.

    See [LLM Providers and Connections](/en/ai-gateway/llm-providers) for the full setup steps.
  </Step>

  <Step title="Point Your OpenAI SDK at Apinizer">
    Configure the OpenAI SDK (or any OpenAI-compatible client) with your gateway's `base_url` and your Apinizer credential key:

    ```python
    from openai import OpenAI

    client = OpenAI(
        api_key="your-apinizer-credential-key",
        base_url="https://your-apinizer-gateway.com/api/ai/v1"
    )
    ```

    No other client-side changes are needed — the SDK talks to Apinizer exactly as it would talk to OpenAI directly.
  </Step>

  <Step title="Send Your First (Streaming) Chat Completion">
    ```python
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": "Hello, Apinizer!"}],
        stream=True
    )

    for chunk in response:
        if chunk.choices[0].delta.content:
            print(chunk.choices[0].delta.content, end="", flush=True)
    ```

    The response streams back chunk-by-chunk as it arrives from the provider — the same shape you'd get calling the provider directly, regardless of which provider actually served the request.
  </Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="AI Gateway Overview" icon="plug" href="/en/ai-gateway/overview">
    See the full request pipeline and feature set
  </Card>
  <Card title="LLM Providers and Connections" icon="database" href="/en/ai-gateway/llm-providers">
    Configure additional providers and deployment types
  </Card>
  <Card title="Model Catalog and Pricing" icon="dollar-sign" href="/en/ai-gateway/model-catalog">
    Browse bundled models and pricing
  </Card>
  <Card title="Routing and Failover" icon="route" href="/en/ai-gateway/routing-and-failover">
    Add backup providers and failover strategies
  </Card>
  <Card title="Token Quotas and Rate Limiting" icon="gauge" href="/en/ai-gateway/token-quotas">
    Cap usage before you go to production
  </Card>
</CardGroup>

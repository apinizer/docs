---
title: "Cost Settings"
description: "You can configure per-model pricing and see costs reconciled by input, output, and cached tokens across your usage reports."
---

import {Card, CardGroup} from '@site/src/components/Card';
import {Steps, Step} from '@site/src/components/Steps';

:::note
Costs are calculated and reported in **USD**, priced per model from your catalog.
:::

## Per-Model Pricing

Every model in your [model catalog](/en/ai-gateway/model-catalog) has a configurable input and output price, expressed per 1M tokens:

```
gpt-4o
  Input:  $0.005 per 1M tokens
  Output: $0.015 per 1M tokens

gpt-4-turbo
  Input:  $0.01 per 1M tokens
  Output: $0.03 per 1M tokens
```

Pricing changes apply going forward — historical usage keeps the price that was in effect when it was recorded.

<Steps>
  <Step title="Open Cost Settings">
    Go to **Admin** → **AI Gateway** → **Cost Settings** (system administrators only).
  </Step>

  <Step title="View Model List">
    A table of every model in use is displayed. Select a row to edit its pricing.
  </Step>

  <Step title="Update Price">
    Edit the input and output price fields. A model with no price configured is treated as free.
  </Step>

  <Step title="Save">
    New requests use the updated price immediately; already-recorded usage is not recalculated.
  </Step>
</Steps>

## Cost Reconciliation

Every request's cost is reconciled from three token counts, priced independently:

<CardGroup cols={1}>
  <Card title="Input Tokens" icon="arrow-right-to-line">
    Tokens sent to the model as the prompt.
  </Card>
  <Card title="Output Tokens" icon="arrow-left-to-line">
    Tokens generated by the model in its response.
  </Card>
  <Card title="Cached Tokens" icon="database">
    Tokens served from the [semantic cache](/en/ai-gateway/semantic-cache) rather than a new model call.
  </Card>
</CardGroup>

Because each is priced separately, your cost reports reflect exactly what was consumed rather than a single blended rate.

## Cost in Reports

Once pricing is configured, cost figures appear throughout [Reports and Analytics](/en/ai-gateway/reports):

- Dashboard cost-to-date
- Cost breakdown by model, project, team, and deployment type
- Exported reports with cost columns

## Permissions

**Cost Settings** can only be viewed and edited by system administrators. Other roles continue to see usage and cost breakdowns in reports, without the ability to change pricing.

## Next Steps

<CardGroup cols={2}>
  <Card title="Token Quotas and Rate Limiting" icon="gauge" href="/en/ai-gateway/token-quotas">
    Pair per-model pricing with monthly USD budgets
  </Card>
  <Card title="Reports and Analytics" icon="chart-bar" href="/en/ai-gateway/reports">
    View cost breakdowns by model, team, and deployment type
  </Card>
</CardGroup>

---
title: "A2A Gateway"
description: "You can create an A2A proxy that publishes itself as an agent for other agents to send tasks to, or forwards tasks through to an external agent — with task lifecycle tracking, streaming, and push notifications built in."
---

import {Card, CardGroup} from '@site/src/components/Card';
import {Steps, Step} from '@site/src/components/Steps';

## Overview

**A2A (Agent2Agent)** is an open protocol that lets AI agents from different providers exchange tasks over a standard interface. In Apinizer, A2A is a **first-class API proxy type** — you create an A2A proxy the same way you'd create a REST, SOAP, or AI proxy, and configure its behavior on a dedicated routing tab. An A2A proxy works in one of two directions:

- **Expose Agent** — the proxy publishes itself as an A2A agent, backed directly by its own LLM configuration
- **Passthrough** — the proxy forwards tasks through to an external A2A agent, applying skill filtering and agent-card rewriting along the way

:::info
**Coming from an older release?** A2A used to be configured on a separate "Inbound Agent" screen with its own authentication settings, reachable at a dedicated path. That model has been replaced entirely — see [Migrating from the Previous Model](#migrating-from-the-previous-model). New to agent-to-agent concepts? See [AI Fundamentals](/en/concepts/core-concepts/ai-fundamentals) for a primer.
:::

## Quick Start

<Steps>
  <Step title="Create an A2A Proxy">
    Create a new **A2A Proxy** the same way you'd create any other API proxy: give it a name and a single client-facing path.
  </Step>

  <Step title="Choose a Routing Mode">
    Open the proxy's **A2A Routing** tab and pick **Expose Agent** or **Passthrough**.
  </Step>

  <Step title="Configure the Mode">
    For Expose Agent, set the LLM backend on the proxy's **AI Routing** tab (required before you can save) and describe the agent's skills on the A2A Routing tab. For Passthrough, pick an outbound A2A connection and, optionally, restrict which of its discovered skills are advertised.
  </Step>

  <Step title="Add Authentication">
    Attach any of Apinizer's regular authentication policies (API Key, JWT, OAuth2, ...) to the proxy — the same way you would on any other proxy type.
  </Step>

  <Step title="Save and Deploy">
    Deploy the proxy. Its client path becomes the single endpoint A2A clients connect to, for both discovery and task submission.
  </Step>

  <Step title="Connect a Client">
    Point an A2A client at the proxy's path — it fetches the agent card from the standard discovery path, then sends tasks over that same path.
  </Step>
</Steps>

## Modes

### Expose Agent

An Expose Agent proxy *is* the agent — there's no separate backend proxy reference to configure. Its LLM backend is the proxy's own AI Routing configuration, so guardrails, PII masking, and the rest of your usual policy pipeline apply to A2A traffic exactly as they do to a regular AI proxy.

### Passthrough

Forwards tasks to an external agent through a stored, encrypted **outbound A2A connection** — the same connection object described below — rewriting the fetched agent card (hostname, advertised authentication scheme, and skill list) before handing it to the caller.

<CardGroup cols={2}>
  <Card title="Automatic Discovery" icon="search">
    Once the outbound connection is saved, Apinizer fetches and previews the target agent's card automatically.
  </Card>
  <Card title="Skill Allow-List" icon="shield-check">
    Leave it empty to advertise every discovered skill, or restrict which ones this proxy exposes.
  </Card>
  <Card title="Health Probe" icon="activity">
    The connection is probed periodically and reports **Healthy**, **Down**, or **Unknown** status with its last response time.
  </Card>
  <Card title="Egress Protection" icon="shield">
    Discovery and task calls can't reach cloud-metadata endpoints, loopback, or link-local addresses — see [Security and Governance](#security-and-governance).
  </Card>
</CardGroup>

### Outbound A2A Connections

An outbound A2A connection is a stored, encrypted reference to an external agent, with its agent card fetched and cached automatically — including an **OAuth2 client-credentials** flow for agents that require one, acquired automatically at runtime. It's the shared backend behind two different things:

- A **Passthrough** A2A proxy forwards every task it receives straight to one.
- The **A2A Agent Call** / **A2A Agent Invoke** policies let a model delegate a sub-task to one as part of any AI proxy's own [tool-call loop](/en/ai-gateway/routing-and-failover#tool-call-loop-agentic-requests) — no A2A proxy required for that path at all.

## Agent Card, Task Lifecycle, and Streaming

### Agent Card

Every A2A proxy publishes a self-describing agent card, unauthenticated, at the proxy's client path plus `/.well-known/agent-card.json` — required so a client can discover the agent before it has credentials. The card includes the agent's name, description, skills, and supported input/output modalities, plus an advertised authentication scheme derived automatically from the proxy's own authentication policies (JWT and OAuth2 are reflected as bearer/OAuth2 schemes, Basic as a basic scheme) — an unauthenticated proxy's card advertises no scheme at all. There's no separate per-agent auth-mode selector anymore, and the card can never claim a scheme the gateway doesn't actually enforce. An authenticated client can also fetch the same card over the JSON-RPC channel.

### Task Submission and Lifecycle

A client starts a task with `SendMessage`; Apinizer accepts it and returns a task ID immediately, then works it in the background.

| State | Meaning |
|---|---|
| Submitted | Received, processing not yet started |
| Working | Being processed |
| Completed | Finished successfully |
| Failed | Ended with an error |
| Canceled | Canceled before completion |

A client polls status with `GetTask`, lists a proxy's tasks with `ListTasks`, and can cancel an in-progress task with `CancelTask` — cancellation actually interrupts the task's processing on the worker, not just its recorded status.

### Streaming

A client can request streaming task updates with `SendStreamingMessage`, delivered over Server-Sent Events. If the connection drops, `SubscribeToTask` replays the task's stored history so the client can pick up from where it left off.

### Push Notifications

For long-running tasks, a client can register a webhook with `CreateTaskPushNotificationConfig` — Apinizer calls it (with an authentication token) once the task reaches a terminal state, so the client doesn't have to keep a connection open or poll for the result.

## Multi-Target Aggregation

A Passthrough proxy can aggregate up to **10** additional outbound A2A connections alongside its primary one (11 in total) behind one endpoint, but only for **discovery**: their skills are merged into a single advertised agent card, with a colliding skill name resolved deterministically the same way MCP tool names are. Sending an actual task is different — the A2A protocol gives a message no field to say which agent it's meant for, so once more than one target is configured, `SendMessage` returns an explicit error rather than guessing which agent should receive it. Configure more than one target only if you want a combined skill catalog for discovery; keep exactly one target if you intend to actually delegate tasks through the proxy.

## Security and Governance

### Authentication

Same model as MCP: whatever policy chain you attach to the proxy. The agent-card discovery endpoint is exempt from it by design.

### Identity-Based Access Control (Passthrough discovery only)

In **Passthrough** mode, you can restrict which skills the proxy advertises to a given credential, role, or organization — a caller not covered by any rule sees no skills once at least one rule is configured. This narrows what appears in the **agent card**, not what a `SendMessage` call is allowed to do: the A2A wire message carries no per-call skill identifier, so — unlike MCP's per-tool-call enforcement — this can't gate an individual task at call time; actual authorization for a delegated message is up to the backend agent. In **Expose Agent** mode this setting isn't applied at all.

### Call Quotas

Cap `SendMessage` volume per minute, hour, or day, scoped per calling credential. Because a message carries no per-call skill identifier, a "per tool" quota on this surface collapses to a single per-proxy bucket rather than a genuinely per-skill one — set the scope to per-credential for meaningful enforcement:

```json
{
  "callQuota": { "scope": "PER_CREDENTIAL", "period": "MINUTE", "limit": 30 }
}
```

### Catalog Drift Protection

The same mechanism as MCP, applied to an outbound A2A connection's advertised skill list instead of a tool catalog: a fingerprint is captured at the last approved discovery, a periodic health check compares the live card against it, and drift either logs and surfaces a warning (the default) or blocks task delegation through that connection until it's re-approved.

### Token Passthrough

The same opt-in model as MCP: off everywhere by default. Forwarding the caller's own token to a backend agent requires explicitly enabling it on the connection, optionally restricted to an allow-list of accepted audiences, with the connection's own credential resolution skipped once a forward succeeds.

### Egress Protection

:::danger
Outbound A2A calls — agent-card fetch, task submission, health probes, and push-notification dispatch — can never reach cloud-metadata endpoints, loopback, link-local, multicast, or carrier-grade-NAT addresses, regardless of configuration. A connection (or, for push notifications, the proxy itself) can optionally be allowed to reach other private-network ranges (for example an in-cluster agent), but that opt-in never lifts the addresses above.
:::

This same protection is shared with [MCP Gateway](/en/ai-gateway/mcp-gateway) outbound connections.

## Logging and Analytics

Every A2A call — agent-card fetch, task submission, streaming, and cancellation — is recorded in your traffic logs with the call direction and, for tasks, the task ID, context ID, and current task state, so a delegated task can be followed end to end. These calls also appear in [trace groups](/en/ai-gateway/tracing-and-replay) alongside the rest of a request's chain, and roll up into your usage [reports](/en/ai-gateway/reports).

## Migrating from the Previous Model

- The separate A2A "Inbound Agent" screen is gone; A2A is now configured directly on an A2A proxy's **A2A Routing** tab.
- Per-agent authentication mode (None / API Key) no longer exists — authentication is whatever policy chain you attach to the proxy, the same as any other proxy type.
- The old dedicated inbound path is gone; clients must be repointed at the A2A proxy's own client path.
- Existing inbound agent definitions aren't carried forward automatically — recreate them as an A2A proxy. Task history for tasks that already ran is retained.
- Outbound A2A connections, their agent-card discovery, and the A2A Agent Call / A2A Agent Invoke policies are unchanged.

## Next Steps

<CardGroup cols={2}>
  <Card title="MCP Gateway" icon="plug" href="/en/ai-gateway/mcp-gateway">
    The other half of agentic connectivity — tool calls instead of delegated tasks
  </Card>
  <Card title="Advanced Guardrails" icon="shield" href="/en/ai-gateway/advanced-guardrails">
    Configure DLP, loop, and off-topic guards
  </Card>
  <Card title="Tracing and Replay" icon="history" href="/en/ai-gateway/tracing-and-replay">
    Follow A2A tasks in the trace chain and re-execute them
  </Card>
  <Card title="Reports and Analytics" icon="chart-bar" href="/en/ai-gateway/reports">
    View usage and cost breakdowns
  </Card>
</CardGroup>

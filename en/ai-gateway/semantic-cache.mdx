---
title: "Semantic Cache"
description: "You can reuse cached responses for identical or highly similar requests to cut latency and cost, while a built-in masking gate keeps sensitive data from ever being cached in its raw form."
---

import {Card, CardGroup} from '@site/src/components/Card';

## Matching Modes

<CardGroup cols={2}>
  <Card title="Exact Match" icon="equal">
    A request that is literally identical to a previous one reuses its cached response.
  </Card>
  <Card title="Vector Similarity" icon="git-compare">
    An opt-in mode that uses the [vector database](/en/ai-gateway/vector-database) to find requests that are near-identical in meaning, even when the wording differs, and reuses the cached answer for them too.
  </Card>
</CardGroup>

Both modes exist to avoid sending the same question to the model twice — lowering both response latency and cost.

## The Masking Gate

:::warning
If a response requires [personal-data masking or data-loss protection](/en/ai-gateway/advanced-guardrails) before it is returned, Apinizer **never stores the raw response** in the cache — only the masked version can be cached. This closes off a scenario where masking could otherwise be bypassed simply by hitting a cache entry recorded before masking ran.
:::

## Cache Lifetime

Each cached entry carries a configurable time-to-live (TTL). Once it expires, it is automatically removed and is no longer returned as a cache hit.

## Monitoring Cache Efficiency

The **Cache Efficiency** report in [Reports and Analytics](/en/ai-gateway/reports) shows how much the cache is saving you:

- Hit rate for exact and similarity matches
- Estimated cost savings, based on what those requests would otherwise have cost against your [model pricing](/en/ai-gateway/cost-settings)

## Next Steps

<CardGroup cols={2}>
  <Card title="Vector Database" icon="database" href="/en/ai-gateway/vector-database">
    Configure the backend that powers similarity matching
  </Card>
  <Card title="Advanced Guardrails" icon="shield-alert" href="/en/ai-gateway/advanced-guardrails">
    Review how masking interacts with cached responses
  </Card>
  <Card title="Cost Settings" icon="dollar-sign" href="/en/ai-gateway/cost-settings">
    See how cache hits factor into cost reporting
  </Card>
  <Card title="Reports and Analytics" icon="chart-bar" href="/en/ai-gateway/reports">
    Monitor cache hit rate and savings over time
  </Card>
</CardGroup>

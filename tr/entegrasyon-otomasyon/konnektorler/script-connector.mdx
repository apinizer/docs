---
title: 'Script Connector'
description: 'JavaScript ve Groovy script\'leri çalıştırın, veri dönüşümleri yapın, özel mantık uygulayın ve Task Flow yeteneklerini genişletin'
---

Script Connector, Task Flow'lar içinde özel JavaScript veya Groovy script'leri çalıştırmanıza olanak tanır. Veri dönüşümleri, özel iş mantığı ve karmaşık hesaplamaları etkinleştirir.

## Genel Bakış

Script Connector, özel kod çalıştırmak için bir script ortamı sağlar ve Task Flow yeteneklerini standart konnektörlerin ötesine genişletmenize olanak tanır.

<CardGroup cols={2}>
  <Card title="JavaScript Desteği" icon="code">
    JavaScript kodu çalıştırın
  </Card>
  <Card title="Groovy Desteği" icon="code">
    Groovy script'leri çalıştırın
  </Card>
  <Card title="Veri Dönüştürme" icon="arrows-rotate">
    Veriyi dönüştürün ve manipüle edin
  </Card>
  <Card title="Özel Mantık" icon="wand-magic-sparkles">
    Özel iş mantığı uygulayın
  </Card>
</CardGroup>

## Yapılandırma

### Script Türü

Script dilini seçin:

* **JavaScript**: JavaScript/ECMAScript
* **Groovy**: Groovy script dili

### Script İçeriği

Script kodunu yazın veya yapıştırın:

**JavaScript Örneği:**

```javascript
// Girdi değişkenlerine erişim
const customerId = variables.customerId;
const orderTotal = variables.orderTotal;

// Hesaplamalar yapın
const discount = orderTotal > 1000 ? orderTotal * 0.1 : 0;
const finalTotal = orderTotal - discount;

// Çıktı değişkenlerini ayarlayın
variables.discountedTotal = finalTotal;
variables.discountAmount = discount;
variables.isPremium = orderTotal > 1000;
```

**Groovy Örneği:**

```groovy
// Girdi değişkenlerine erişim
def customerId = variables.customerId
def orderTotal = variables.orderTotal

// Hesaplamalar yapın
def discount = orderTotal > 1000 ? orderTotal * 0.1 : 0
def finalTotal = orderTotal - discount

// Çıktı değişkenlerini ayarlayın
variables.discountedTotal = finalTotal
variables.discountAmount = discount
variables.isPremium = orderTotal > 1000
```

## Değişken Erişimi

### Değişken Okuma

Task Flow değişkenlerine erişin:

**JavaScript:**

```javascript
const value = variables.variableName;
const nested = variables.customer.address.city;
```

**Groovy:**

```groovy
def value = variables.variableName
def nested = variables.customer.address.city
```

### Değişken Yazma

Task Flow değişkenlerini ayarlayın:

**JavaScript:**

```javascript
variables.newVariable = "value";
variables.customer.status = "active";
```

**Groovy:**

```groovy
variables.newVariable = "value"
variables.customer.status = "active"
```

## Veri Dönüştürme

### JSON Dönüştürme

JSON verisini dönüştürün:

**JavaScript:**

```javascript
// Nesneyi dönüştür
const transformed = {
  id: customer.id,
  fullName: `${customer.firstName} ${customer.lastName}`,
  email: customer.email.toLowerCase()
};

variables.transformedCustomer = transformed;
```

### Dizi İşlemleri

Dizileri işleyin:

**JavaScript:**

```javascript
// Dizi map
const emails = customers.map(c => c.email);

// Dizi filtreleme
const activeCustomers = customers.filter(c => c.status === 'active');

// Dizi azaltma
const total = orders.reduce((sum, order) => sum + order.total, 0);
```

### String İşlemleri

String'leri manipüle edin:

**JavaScript:**

```javascript
const fullName = `${firstName} ${lastName}`;
const upperCase = name.toUpperCase();
const trimmed = value.trim();
```

## Yaygın Desenler

### Veri Doğrulama

Veriyi doğrulayın:

**JavaScript:**

```javascript
function validateEmail(email) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

if (!validateEmail(variables.email)) {
  throw new Error("Geçersiz e-posta adresi");
}
```

### Koşullu Mantık

Koşullu mantık uygulayın:

**JavaScript:**

```javascript
if (variables.orderTotal > 1000) {
  variables.discount = variables.orderTotal * 0.1;
  variables.customerType = "premium";
} else {
  variables.discount = 0;
  variables.customerType = "standard";
}
```

### Döngüler

Koleksiyonlar üzerinde yineleme yapın:

**JavaScript:**

```javascript
const results = [];
for (const item of variables.items) {
  const processed = processItem(item);
  results.push(processed);
}
variables.processedItems = results;
```

## Hata Yönetimi

### Hata Fırlatma

Çalıştırmayı durdurmak için hata fırlatın:

**JavaScript:**

```javascript
if (!variables.requiredField) {
  throw new Error("Gerekli alan eksik");
}
```

### Try-Catch

Hataları yönetin:

**JavaScript:**

```javascript
try {
  const result = riskyOperation();
  variables.result = result;
} catch (error) {
  variables.error = error.message;
  variables.result = null;
}
```

## En İyi Uygulamalar

<CardGroup cols={2}>
  <Card title="Script'leri Basit Tutun" icon="code">
    Script'leri odaklı ve basit tutun
  </Card>
  <Card title="Hataları Yönetin" icon="exclamation-triangle">
    Her zaman potansiyel hataları yönetin
  </Card>
  <Card title="Girdiyi Doğrulayın" icon="check-circle">
    İşlemeden önce girdi verisini doğrulayın
  </Card>
  <Card title="Kodu Dokümante Edin" icon="file-lines">
    Karmaşık mantığı açıklamak için yorumlar ekleyin
  </Card>
  <Card title="Script'leri Test Edin" icon="flask">
    Dağıtımdan önce script'leri kapsamlı bir şekilde test edin
  </Card>
  <Card title="Performans" icon="gauge">
    Performans etkilerini göz önünde bulundurun
  </Card>
</CardGroup>

## İlgili Konular

<CardGroup cols={2}>
  <Card title="Task Flow Tasarımı" icon="diagram-project" href="/tr/entegrasyon-otomasyon/task-flow-tasarimi">
    Task Flow tasarımını öğrenin
  </Card>
  <Card title="Variables & Data Passing" icon="arrows-rotate" href="/tr/entegrasyon-otomasyon/variables-data-passing">
    Değişken yönetimini anlayın
  </Card>
</CardGroup>

---
title: "Byte Array Content Types"
description: "Content-Type header değerine göre mesajların Byte Array olarak kullanılmasını sağlayacak değer kümesi oluşturabilir. Gelen ve dönen mesajların gövdelerinin Byte Array olarak değerlendirilmesini yapılandırabilir."
---

<Info>
Normalde gelen (request) ve dönen (response) mesajların gövdeleri (body) text olarak değerlendirilir. Eğer Content-Type değeri bu liste içinde ise içerik Byte Array değeri olarak ele alınır.
</Info>

## Yeni Bir Byte Array Content Type Oluşturma

**Yönetim → Sistem Ayarları → Ön Tanımlı Değerler → Byte Array Content Types** ekranından **+Yeni** butonuna tıklayınız.

**Byte Array Content Type oluşturma ayarlarını içeren görsele aşağıda yer verilmiştir:**

<img
  src="/images/admin/system-settings/on-tanimli-degerler/byte-array-content-type-olusturma.png"
  alt="Byte Array Content Type Oluşturma"
  width="1000"
  height="600"
/>

Byte Array Content Type oluşturma konfigürasyonu için kullanılan alanlar aşağıdaki tabloda görülmektedir.

| Alan | Açıklama |
| --- | --- |
| Değer *(Value)* | Byte Array Content Type Değerinin girildiği alandır. |

---
title: "Çok-Modaliteli Uç Noktalar (Ses ve Görsel)"
description: "OpenAI-uyumlu ses-metin (STT), metin-ses (TTS) ve görsel üretim uç noktalarını Apinizer AI Gateway üzerinden kullanın; kullanım ve maliyet metrikleri diğer modellerle aynı raporlama altyapısında izlenir."
---

import {Card} from '@site/src/components/Card';
import {Steps, Step} from '@site/src/components/Steps';

## Genel Bakış

Apinizer AI Gateway, metin tabanlı sohbet tamamlama uç noktalarına ek olarak üç yeni modaliteyi destekler:

- **Ses-Metin (STT — Speech to Text)** — `/v1/audio/transcriptions`
- **Metin-Ses (TTS — Text to Speech)** — `/v1/audio/speech`
- **Görsel Üretim (Image Generation)** — `/v1/images/generations`

Her üç uç nokta da **OpenAI-uyumlu istek/yanıt formatını** kullanır; mevcut OpenAI SDK istemcileriniz `base_url` değişikliği dışında kod değişikliği gerektirmeden çalışır.

:::info

**Video modalitesi** bu sürüme dahil değildir — sonraki bir faz için planlanmıştır.

:::

## Ses-Metin (STT)

Ses dosyasını metne çevirir:

```python
from openai import OpenAI

client = OpenAI(
    api_key="apinizer-kimlik-bilgisi-anahtarınız",
    base_url="https://apinizer-gateway-adresiniz.com/api/ai/v1"
)

with open("kayit.mp3", "rb") as audio_file:
    transcript = client.audio.transcriptions.create(
        model="whisper-1",
        file=audio_file
    )

print(transcript.text)
```

- İstek **çok parçalı (multipart/form-data)** formatında gönderilir
- Kullanım metrikleri **ses süresine (duration)** dayalı hesaplanır (token sayımı yerine)
- Yanıt metni, mevcut **PII maskeleme** politikasından geçirilebilir — transkript içinde tespit edilen kişisel veriler diğer metin uç noktalarıyla aynı kurallarla maskelenir

## Metin-Ses (TTS)

Metni sese çevirir; yanıt **ikili (binary) ses akışı** olarak döner:

```python
response = client.audio.speech.create(
    model="tts-1",
    voice="alloy",
    input="Merhaba, bu bir test mesajıdır."
)

response.stream_to_file("cikti.mp3")
```

:::note

TTS yanıtı JSON değil, doğrudan ses dosyası (binary) olarak döner — gateway bu yanıt tipini şeffaf şekilde iletir (pass-through), içerik türü (content-type) korunur.

:::

## Görsel Üretim

Metin isteminden görsel üretir:

```python
response = client.images.generate(
    model="gpt-image-1",
    prompt="Deniz kenarında gün batımı, dijital sanat",
    n=1,
    size="1024x1024"
)

print(response.data[0].url)
```

## Fiyatlandırma ve Kullanım Metrikleri

Yeni modaliteler, mevcut **AI Maliyet Ayarları** ve **Raporlar** altyapısına entegre edilmiştir:

| Modalite | Fiyatlandırma Birimi | Kullanım Metriği |
|----------|----------------------|-------------------|
| STT (whisper) | Dakika başına | Ses süresi (saniye/dakika) |
| TTS | Karakter başına | Girdi karakter sayısı |
| Görsel Üretim | Görsel başına (boyuta göre) | Üretilen görsel sayısı |

Bu metrikler **Raporlar ve Analitik** ekranında diğer LLM modelleriyle birlikte, kişi/ekip/model kırılımında görüntülenebilir.

:::tip

Yeni modeller (whisper, tts, gpt-image gibi) katalogda **önceden tanımlı fiyatlarla** gelir; ihtiyaç halinde model fiyatlarını **AI Maliyet Ayarları** sayfasından özelleştirebilirsiniz.

:::

## Uç Nokta Yönlendirme

Gateway, gelen isteğin hedef yolunu (`/v1/chat/completions`, `/v1/audio/transcriptions`, `/v1/audio/speech`, `/v1/images/generations`) otomatik olarak tanır ve modaliteye özgü işleme hattına (binary yanıt desteği, multipart ayrıştırma, süre bazlı kullanım hesaplama dahil) yönlendirir — ek yapılandırma gerekmez, aynı AI proxy üzerinden tüm modaliteler sunulabilir.

{/* TODO: ekran görüntüsü — model kataloğunda whisper/tts/gpt-image fiyat alanları */}

## Sonraki Adımlar

- **[AI Maliyet Ayarları](/tr/ai-gateway/maliyet-ayarlari)** — Modalite bazlı fiyatlandırmayı özelleştirin
- **[Raporlar ve Analitik](/tr/ai-gateway/raporlar)** — Modalite bazlı kullanım ve maliyeti izleyin
- **[Gelişmiş Korumalar (Guardrails)](/tr/ai-gateway/gelismis-korumalar)** — STT yanıtlarında PII maskeleme dahil koruma katmanlarını yapılandırın

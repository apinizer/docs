---
title: "SSL/TLS ile Apinizer Modüllerini Başlatma"
description: "Apinizer modüllerini SSL/TLS ile başlatma yapılandırması"
---

# SSL/TLS ile Apinizer Modüllerini Başlatma

Bu sayfa, Apinizer modüllerini SSL/TLS ile başlatma yapılandırmasını açıklamaktadır.

## Genel Bakış

Apinizer modüllerini (Manager, Gateway, Portal) SSL/TLS sertifikaları ile güvenli şekilde başlatabilirsiniz.

## Yapılandırma

### 1. Sertifika Hazırlığı

Sertifikaları hazırlayın:

- SSL/TLS sertifikası oluşturun veya temin edin
- Private key'i hazırlayın
- Certificate chain'i hazırlayın

### 2. Sertifika Formatı Dönüşümü

Gerekirse sertifika formatını dönüştürün:

- PFX'ten JKS'e dönüştürme
- PEM'den JKS'e dönüştürme

### 3. Kubernetes Secret Oluşturma

Sertifikaları Kubernetes secret olarak oluşturun:

```bash
kubectl create secret tls apinizer-tls \
  --cert=certificate.crt \
  --key=private.key \
  -n apinizer
```

### 4. Deployment Yapılandırması

Deployment yapılandırmasında SSL ayarlarını yapın:

```yaml
env:
  - name: SSL_ENABLED
    value: "true"
  - name: SSL_KEY_STORE
    value: "/etc/ssl/certs/keystore.jks"
  - name: SSL_KEY_STORE_PASSWORD
    valueFrom:
      secretKeyRef:
        name: apinizer-tls
        key: password
  - name: SERVER_PORT
    value: "8443"
```

## İlgili Kaynaklar

- [API Manager SSL](/tr/bakim-operasyon/yonetici-kilavuzlari/api-manager-ssl)
- [PFX JKS Dönüştürme](/tr/bakim-operasyon/yonetici-kilavuzlari/pfx-jks-donusturme)

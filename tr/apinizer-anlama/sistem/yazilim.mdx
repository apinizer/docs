---
title: "Yazılım Gereksinimleri"
description: 'OS (RHEL, Ubuntu, Oracle Linux), Kernel ayarları'
---

Bu sayfa, Apinizer platformunun çalışması için gerekli yazılım gereksinimlerini ve işletim sistemi konfigürasyonlarını içerir.

## Desteklenen İşletim Sistemleri

Apinizer Kubernetes üzerinden çalıştığından, Kubernetes kurulu olan tüm işletim sistemlerini destekler. Ancak dokümantasyondaki kurulumlar Linux tabanlı işletim sistemlerine yöneliktir.

<CardGroup cols={3}>
  <Card title="Red Hat Enterprise Linux" icon="linux">
    * **Versiyonlar**: RHEL 7.x, 8.x, 9.x
    * **Önerilen**: RHEL 8.x veya 9.x
    * **Kurulum**: [RHEL Kurulum Kılavuzu](/tr/kurulum-surum-yukseltme/kubernetes-kurulumu/red-hat-enterprise-linux-veya-rocky-linux-uzerinde-kubernetes-kurulumu)
  </Card>
  <Card title="Ubuntu" icon="ubuntu">
    * **Versiyonlar**: Ubuntu 20.04 LTS, 22.04 LTS
    * **Önerilen**: Ubuntu 22.04 LTS
    * **Kurulum**: [Ubuntu Kurulum Kılavuzu](/tr/kurulum-surum-yukseltme/kubernetes-kurulumu/ubuntu-uzerinde-kubernetes-kurulumu)
  </Card>
  <Card title="Oracle Linux" icon="linux">
    * **Versiyonlar**: OL 7.x, 8.x, 9.x
    * **Önerilen**: OL 8.x veya 9.x
    * **Kurulum**: RHEL kurulum kılavuzuna benzer
  </Card>
</CardGroup>

<Info>
  **Not**: Apinizer Kubernetes üzerinden çalıştığı için, Kubernetes destekleyen diğer Linux dağıtımları da kullanılabilir (CentOS, Rocky Linux, SUSE Linux Enterprise Server vb.).
</Info>

## Kubernetes Gereksinimleri

Apinizer imajları DockerHub üzerinden rootless sağlandığından, herhangi bir container orchestration tabanlı platformda çalıştırılabilir.

<CardGroup cols={2}>
  <Card title="Kubernetes" icon="layer-group">
    * **Versiyon**: 1.20+ (önerilen: 1.24+)
    * **Versiyon bağımlılığı**: Yok
    * **Özellikler**: 
      * Namespace desteği
      * RBAC desteği
      * Network policies
      * Storage classes
  </Card>
  <Card title="Alternatif Platformlar" icon="sitemap">
    * **OpenShift**: 4.x
    * **Tanzu**: v1.x
    * **RKE2**: 1.x
    * **AKS/EKS/GKE**: Tüm versiyonlar
  </Card>
</CardGroup>

## Veritabanı Gereksinimleri

### MongoDB

<CardGroup cols={2}>
  <Card title="Versiyon" icon="database">
    * **Versiyon**: Herhangi bir versiyon bağımlılığı yok
    * **Önerilen**: MongoDB 4.4+ veya 5.0+
    * **Önemli**: Replica Set olarak yapılandırılmalıdır
  </Card>
  <Card title="Yapılandırma" icon="gear">
    * **Replica Set**: Zorunlu (tek node dahi olsa)
    * **Standalone**: Kullanılmamalıdır
    * **Sharding**: Büyük kurulumlar için opsiyonel
  </Card>
</CardGroup>

<Warning>
  **Kritik**: MongoDB mutlaka **Replica Set** olarak yapılandırılmalıdır. Tek node dahi olsa replica set olarak kurulmalıdır. Standalone instance kullanılmamalıdır.
</Warning>

### Elasticsearch

<CardGroup cols={2}>
  <Card title="Versiyon" icon="magnifying-glass">
    * **Resmi Desteklenen**: 7.9.2
    * **Önerilen**: 7.9.2 veya 7.x serisi
    * **8.x**: Test edilmiş ancak resmi destek yok
  </Card>
  <Card title="Yapılandırma" icon="gear">
    * **Cluster Mode**: Önerilir (minimum 3 node)
    * **Index Lifecycle Management**: Aktif olmalı
    * **Snapshot**: Yedekleme için önerilir
  </Card>
</CardGroup>

## Kernel Ayarları

Linux kernel'inin belirli parametrelerinin yapılandırılması gerekir.

### sysctl Ayarları

Aşağıdaki ayarlar `/etc/sysctl.conf` dosyasına eklenmelidir:

```bash
# Network ayarları
net.core.somaxconn = 32768
net.core.netdev_max_backlog = 5000
net.ipv4.tcp_max_syn_backlog = 8192
net.ipv4.tcp_fin_timeout = 30
net.ipv4.tcp_keepalive_time = 300
net.ipv4.tcp_keepalive_probes = 5
net.ipv4.tcp_keepalive_intvl = 15

# Memory ayarları
vm.swappiness = 10
vm.max_map_count = 262144
vm.dirty_ratio = 15
vm.dirty_background_ratio = 5

# File descriptor limits
fs.file-max = 2097152
fs.nr_open = 2097152
```

Ayarları uygulamak için:
```bash
sudo sysctl -p
```

### File Descriptor Limits

`/etc/security/limits.conf` dosyasına eklenmelidir:

```bash
* soft nofile 65536
* hard nofile 65536
* soft nproc 32768
* hard nproc 32768
```

### Systemd Limits

`/etc/systemd/system.conf` dosyasına eklenmelidir:

```bash
DefaultLimitNOFILE=65536
DefaultLimitNPROC=32768
```

Değişiklikleri uygulamak için:
```bash
sudo systemctl daemon-reload
```

## Network Ayarları

### Jumbo Frames

Cluster içi iletişim için jumbo frames önerilir:

```bash
# /etc/sysconfig/network-scripts/ifcfg-eth0 (RHEL/CentOS)
MTU=9000

# /etc/netplan/01-netcfg.yaml (Ubuntu)
network:
  version: 2
  ethernets:
    eth0:
      mtu: 9000
```

### Network Bonding (Opsiyonel)

Network redundancy için bonding yapılandırması:

```bash
# /etc/sysconfig/network-scripts/ifcfg-bond0
DEVICE=bond0
TYPE=Bond
BONDING_MASTER=yes
BONDING_OPTS="mode=802.3ad miimon=100"
MTU=9000
```

## Disk ve Dosya Sistemi

### Dosya Sistemi

* **Önerilen**: ext4 veya xfs
* **Mount Options**: `noatime,nodiratime`

Örnek `/etc/fstab` satırı:
```bash
/dev/sdb1 /data ext4 defaults,noatime,nodiratime 0 2
```

### Disk I/O Scheduler

SSD için `noop` veya `none` scheduler önerilir:

```bash
# Geçici olarak
echo noop > /sys/block/sda/queue/scheduler

# Kalıcı olarak /etc/rc.local veya systemd service
```

## Güvenlik Ayarları

### SELinux (RHEL/CentOS)

SELinux'un `permissive` veya `disabled` modda olması gerekebilir:

```bash
# Geçici olarak
sudo setenforce 0

# Kalıcı olarak /etc/selinux/config
SELINUX=permissive
```

<Warning>
  **Güvenlik Notu**: SELinux'u disabled yapmak güvenlik riski oluşturabilir. Mümkünse permissive modda çalıştırın ve gerekli policy'leri oluşturun.
</Warning>

### Firewall

Gerekli portların açılması:

```bash
# firewalld (RHEL/CentOS)
sudo firewall-cmd --permanent --add-port=6443/tcp  # Kubernetes API
sudo firewall-cmd --permanent --add-port=10250/tcp # Kubelet
sudo firewall-cmd --permanent --add-port=10259/tcp # kube-scheduler
sudo firewall-cmd --permanent --add-port=10257/tcp # kube-controller-manager
sudo firewall-cmd --reload

# ufw (Ubuntu)
sudo ufw allow 6443/tcp
sudo ufw allow 10250/tcp
sudo ufw allow 10259/tcp
sudo ufw allow 10257/tcp
```

## Zaman Senkronizasyonu

NTP/Chrony yapılandırması zorunludur:

```bash
# chronyd yapılandırması
sudo systemctl enable chronyd
sudo systemctl start chronyd

# NTP server'ları /etc/chrony.conf
server 0.pool.ntp.org iburst
server 1.pool.ntp.org iburst
server 2.pool.ntp.org iburst
server 3.pool.ntp.org iburst
```

## Yazılım Checklist

- [ ] İşletim sistemi kuruldu (RHEL/Ubuntu/Oracle Linux)
- [ ] Kernel ayarları yapılandırıldı (sysctl)
- [ ] File descriptor limits ayarlandı
- [ ] Network ayarları yapılandırıldı (MTU, bonding)
- [ ] Disk ve dosya sistemi yapılandırıldı
- [ ] Güvenlik ayarları yapılandırıldı (SELinux, firewall)
- [ ] Zaman senkronizasyonu yapılandırıldı (NTP/Chrony)
- [ ] Kubernetes kuruldu ve yapılandırıldı
- [ ] MongoDB kuruldu (Replica Set)
- [ ] Elasticsearch kuruldu
- [ ] Tüm servisler çalışıyor ve test edildi

## Sonraki Adımlar

<CardGroup cols={2}>
  <Card title="Kurulum Kılavuzu" icon="download" href="/tr/kurulum-surum-yukseltme/kurulum/kurulum-oncesi-verilmesi-gereken-kararlar">
    Kurulum adımlarına geçin
  </Card>
  <Card title="Kubernetes Kurulumu" icon="layer-group" href="/tr/kurulum-surum-yukseltme/kubernetes-kurulumu/giris">
    Kubernetes kurulumunu yapın
  </Card>
  <Card title="MongoDB Kurulumu" icon="database" href="/tr/kurulum-surum-yukseltme/mongodb-kurulumu/giris">
    MongoDB kurulumunu yapın
  </Card>
  <Card title="Elasticsearch Kurulumu" icon="magnifying-glass" href="/tr/kurulum-surum-yukseltme/elasticsearch-kurulumu/giris">
    Elasticsearch kurulumunu yapın
  </Card>
</CardGroup>

<Note>
  **Önemli**: Tüm kernel ve sistem ayarları production ortamlarına geçmeden önce test ortamlarında doğrulanmalıdır. Bazı ayarlar sistem davranışını önemli ölçüde etkileyebilir.
</Note>
